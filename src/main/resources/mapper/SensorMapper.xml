<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bigData.mapper.SensorDataMapper">


    <insert id="insert" parameterType="com.example.bigData.entity.SensorData">
        INSERT INTO sensor_data(
        id,
        device_id,
        temperature,
        humidity,create_time
        ) VALUES(
        #{id},
        #{deviceId},
        #{temperature},
        #{humidity},
        #{createTime}
        )
    </insert>


</mapper>
